import axios from 'axios';
require('dotenv').config();

class RobloxClient {
    cookie: string;

    constructor(cookie: string) {
        this.cookie = cookie;
    }

    
}

export { RobloxClient };
